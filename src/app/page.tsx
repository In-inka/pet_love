const Home = async () => {
  return (
    <main className="flex min-h-[100vh] w-full flex-col items-center justify-center gap-2 bg-graphite">
      <h1>Todo List</h1>
    </main>
  );
};

export default Home;
